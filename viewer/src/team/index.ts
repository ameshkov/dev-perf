/**
 * Public API of the viewer's team section: the section component
 * consumed by the dashboard. External code imports from this barrel
 * only; the block builders are internal to the module.
 */
export { TeamSection } from './team-section.js';

/**
 * Public API of the viewer's app module: the root component mounted
 * by the entry point. External code imports from this barrel only;
 * the hero, upload panel, dashboard and loader are internal to the
 * module.
 */
export { App } from './app.js';

/**
 * Public API of the viewer's individual section: the section
 * component consumed by the dashboard. External code imports from
 * this barrel only; the per-user block builders, the detail view and
 * the contribution cards are internal to the module.
 */
export { IndividualSection } from './user-section.js';
